# Include the generated source list
include(${CMAKE_SOURCE_DIR}/cmake/source_lists/GODOT_MAIN_sourcelist.cmake)

# Note: The main executable is built in the root CMakeLists.txt
# We don't need to create a separate target here. 